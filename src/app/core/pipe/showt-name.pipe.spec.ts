import { ShowtNamePipe } from './showt-name.pipe';

describe('ShowtNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowtNamePipe();
    expect(pipe).toBeTruthy();
  });
});
